<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- TARZO Custom Styles (minimal XML, app uses Compose for UI) -->

    <style name="TarzoTextSectionHeading" parent="android:TextAppearance.Medium">
        <item name="android:textColor">@color/tarzo_text_primary</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="TarzoTextBody" parent="android:TextAppearance">
        <item name="android:textColor">@color/tarzo_text_secondary</item>
    </style>

    <style name="TarzoTextCaption" parent="android:TextAppearance.Small">
        <item name="android:textColor">@color/tarzo_text_secondary</item>
    </style>

</resources>
